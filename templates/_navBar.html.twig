<nav class="navbar navbar-expand-lg navbar-light bg-96CEB4">

    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
                aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav mx-auto">
                {% if app.user and 'ROLE_ADMIN' in app.user.roles %}
                    <li class="nav-item">
                        <a class="nav-link mx-2 active" aria-current="page" href="#" title="Ville">Villes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link mx-2 active" href="#">Sites</a>
                    </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link mx-2 active" href="{{ path('app_main') }}" tabindex="-1" aria-disabled="true"
                       title="Sites">Sorties</a>
                </li>

                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link " aria-current="page" href="{{ path('sortie_creer') }}" title="Sorties">Proposer
                            un évènement</a>
                    </li>
                    <li class="nav-item d-none d-lg-block">
                        <a class="nav-link mx-2" href="#">
                            <img src="{{ asset('img/logo.png') }}" alt="logo">
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link mx-2 active" aria-current="page" href="{{ path('app_profil') }}" title="Profil">Mon
                            profil</a>
                    </li>


                    <li class="nav-item">
                        <a class="nav-link mx-2 active" href="{{ path('app_logout') }}" tabindex="-1" aria-disabled="true"
                           title="Se déconnecter">Se déconnecter</a>
                    </li>
                    <li class="nav-item"><a class="text-black " tabindex="-1" aria-disabled="true"
                                            title="Bienvenue pseudo">Bienvenue {{ app.user.pseudo }}</a></li>

                {% else %}
                    <li class="nav-item">
                        <a class="nav-link mx-2 active" href="{{ path('app_login') }}" tabindex="-1" aria-disabled="true"
                           title="Connection">Connection</a>
                    </li>

                {% endif %}
            </ul>
        </div>
    </div>
</nav>
