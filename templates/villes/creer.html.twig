{% extends 'base.html.twig' %}

{% block body %}
    <div>
        {{ form_start(form) }}
        {{ form_widget(form) }}
            <button class="btn btn-secondary w-100 mb-3">Ajouter</button>
        {{ form_end(form) }}


    </div>

{% endblock %}

{% block title %}

{% endblock %}

